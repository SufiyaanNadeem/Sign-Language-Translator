import { Tensor1D, Tensor3D } from 'deeplearn';
export declare class KNNImageClassifier {
    private numClasses;
    private k;
    private squeezeNet;
    private trainLogitsMatrix;
    private classLogitsMatrices;
    private classExampleCount;
    private varsLoaded;
    private squashLogitsDenominator;
    constructor(numClasses: number, k: number);
    load(): Promise<void>;
    clearClass(classIndex: number): void;
    addImage(image: Tensor3D, classIndex: number): void;
    predict(image: Tensor3D): Tensor1D;
    predictClass(image: Tensor3D): Promise<{
        classIndex: number;
        confidences: number[];
    }>;
    getClassExampleCount(): number[];
    private clearTrainLogitsMatrix();
    private concatWithNulls(ndarray1, ndarray2);
    private normalizeVector(vec);
    private getNumExamples();
    dispose(): void;
}
